package com.abood.taskmanager.entity;

/**
 * - ADMIN: Full control over workspace (add/remove users, manage projects)
 * - MEMBER: Can view and work on assigned tasks
 */
public enum Role {
    ADMIN,
    MEMBER
}
